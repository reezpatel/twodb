import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const contentManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/content",
	version: "1.0.0",
};
