import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const meetingsManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/meetings",
	version: "1.0.0",
};
