import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const zaiManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/agent-provider-zai",
	version: "1.0.0",
};
