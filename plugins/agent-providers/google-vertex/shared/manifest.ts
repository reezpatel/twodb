import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const googleVertexManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/agent-provider-google-vertex",
	version: "1.0.0",
};
