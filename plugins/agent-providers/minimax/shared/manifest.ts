import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const minimaxManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/agent-provider-minimax",
	version: "1.0.0",
};
