import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const togetherManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/agent-provider-together",
	version: "1.0.0",
};
