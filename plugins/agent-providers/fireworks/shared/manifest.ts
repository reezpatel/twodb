import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const fireworksManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/agent-provider-fireworks",
	version: "1.0.0",
};
