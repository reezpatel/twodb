import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const clineManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/agent-provider-cline",
	version: "1.0.0",
};
