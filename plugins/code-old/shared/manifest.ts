import type { PluginManifest } from "@twodb/contracts";
import { PLUGIN_ID } from "./constants";

export const codeManifest: PluginManifest = {
	id: PLUGIN_ID,
	name: "@twodb/code",
	version: "1.0.0",
};
