import css from "styled-jsx/css";

export const navPanelFooterStyles = css`
  .tw-navpanel__footer {
    margin-top: auto;
    border-top: 1px solid var(--line);
    padding-top: 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
`;
